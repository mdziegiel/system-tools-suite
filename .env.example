# Optional API keys for IOC Scanner. Leave blank to use local classification only.
VIRUSTOTAL_API_KEY=
ABUSEIPDB_API_KEY=

# Runtime paths inside the container. The defaults are sane.
DATA_DIR=/data
DIST_DIR=/app/dist
